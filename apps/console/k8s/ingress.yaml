apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: PROJECT_NAME
  namespace: APP_NAMESPACE
spec:
  ingressClassName: traefik
  rules:
    - host: SERVICE_PREFIXconsole.PLATFORM_DOMAIN
      http:
        paths:
          - path: /ws/terminal
            pathType: Prefix
            backend:
              service:
                name: PROJECT_NAME
                port:
                  number: 3001
          - path: /ws/devpod
            pathType: Prefix
            backend:
              service:
                name: PROJECT_NAME
                port:
                  number: 3001
          - path: /
            pathType: Prefix
            backend:
              service:
                name: PROJECT_NAME
                port:
                  number: 80
