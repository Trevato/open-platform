# Open Platform Configuration
# Copy to open-platform.yaml and customize before deploying.

domain: myplatform.example.com

# Optional: prefix for service subdomains (for multi-tenant hosting)
# When set, services become {prefix}forgejo.{domain}, {prefix}ci.{domain}, etc.
# Leave unset or empty for standard behavior (forgejo.{domain}, ci.{domain}).
# service_prefix: myteam-

admin:
  username: opadmin    # "admin" is a reserved name in Forgejo
  email: admin@example.com

tls:
  # letsencrypt: Real certs via cert-manager (recommended for public deployments)
  # selfsigned:  Self-signed CA (for air-gapped or local development)
  # cloudflare:  Cloudflare tunnel handles TLS at edge (self-signed in-cluster)
  mode: letsencrypt
  email: admin@example.com

# Network mode: how Traefik receives external traffic
# host:         DaemonSet with hostNetwork (default — binds ports 80/443 on node)
# loadbalancer: Deployment behind MetalLB L2 VIP (for dedicated edge VLAN with DNAT)
#
# When using loadbalancer mode, k3s servicelb must be disabled (handled by NixOS module,
# or manually add --disable servicelb to k3s config for non-NixOS hosts).
#
# network:
#   mode: loadbalancer
#   traefik_ip: 10.0.16.10          # Pinned VIP (must match firewall DNAT rule)
#   address_pool: 10.0.16.0/24      # MetalLB shared-infra pool (CIDR or range)
#   interface: eno3                  # NIC for L2 ARP (VLAN 101 edge interface)

# Optional: Cloudflare tunnel (only used when tls.mode == cloudflare)
# cloudflare:
#   account_tag: ""
#   tunnel_id: ""
#   tunnel_secret: ""

# Email: Bundled Mailpit (dev) or external SMTP (production)
# Mailpit deploys automatically at mail.{domain} for catching all emails.
# To use an external SMTP server instead, uncomment the smtp section:
# smtp:
#   host: smtp.example.com
#   port: 587
#   username: user@example.com
#   password: secret
#   from: noreply@example.com

# Jitsi Meet: Video conferencing (optional)
# jitsi:
#   enabled: true
#   allow_guests: true             # Allow unauthenticated guests to join meetings
#   jvb_advertise_ip: 10.0.0.44   # Required: server IP for media traffic (not proxied by Cloudflare)

# Zulip: Team messaging (optional)
# zulip:
#   enabled: true

# Instance provisioning (optional — enables child platform creation via vCluster)
# provisioner:
#   enabled: true
