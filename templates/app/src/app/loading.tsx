export default function Loading() {
  return (
    <main className="container">
      <p style={{ color: "var(--text-muted)", fontSize: 14, paddingTop: 40 }}>
        Loading...
      </p>
    </main>
  );
}
